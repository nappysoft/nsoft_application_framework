maker.namespace('apps.game'); 
(function game () {
	var that = this,
		application = undefined,
		config = {};

	function load (applicationObj,optionsObj,progressUpdater) {

	};
	return {
		config: config,
		load: load
	}
}.apply(maker.apps.game));
